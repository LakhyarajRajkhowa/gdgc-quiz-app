package com.example.quizapp.presentation.navigation

enum class BottomNavItem(val route: String) {
    HOME(NavRoutes.HOME),
    LIBRARY(NavRoutes.LIBRARY),
    LEADERBOARD(NavRoutes.LEADERBOARD),
    ME(NavRoutes.PROFILE)
}